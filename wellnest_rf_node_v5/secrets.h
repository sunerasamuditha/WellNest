/* =============================================================================
 *  secrets.h — WellNest RF presence node
 *
 *  Put this file in the SAME FOLDER as wellnest_rf_node_v3.ino
 *  (Arduino IDE will show it as a tab next to the sketch).
 *
 *  ADD THIS LINE TO .gitignore BEFORE YOUR FIRST COMMIT:
 *      secrets.h
 *
 *  Commit a copy named secrets.h.example with dummy values so the repo is
 *  still buildable by anyone who clones it.
 * ========================================================================== */

#pragma once

#define WIFI_SSID      "SUNERA"
#define WIFI_PASSWORD  "0707305137"
